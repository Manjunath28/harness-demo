apiVersion: v1
kind: Service
metadata:
  name: harness-cicd-app
  namespace: harness-demo
  labels:
    app: harness-cicd-app
spec:
  type: NodePort
  selector:
    app: harness-cicd-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
